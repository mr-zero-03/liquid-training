{% assign arrayString = "Mike,Carlos,Pedro,Alfonso,Juan" %}
{% assign array = arrayString | split: "," %}

<ul>
{% for name in array %}
  {% if name.size < 5 %}
    <li>{{ name }}</li>
  {% endif %}
{% endfor %}
</ul>
